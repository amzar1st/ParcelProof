# Superseded v1 verification

The v1 contract used the documented `Response.status_code`, which does not exist in the pinned deployed SDK. Its evidence path therefore fell back to INCONCLUSIVE. These receipts retain the successful payment, refund, evidence and timeout history, and the failed positive synthetic test. This deployment is superseded; do not interpret its inconclusive results as successful external evidence interpretation.
